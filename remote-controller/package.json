{
  "name": "remote-controller",
  "version": "1.0.0",
  "description": "Lightweight mobile remote controller for AI Institutional Persona",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3001",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3001"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
